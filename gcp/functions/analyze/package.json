{
  "name": "analyze-function",
  "version": "1.0.0",
  "description": "Cloud Function for resume analysis with OpenAI",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=analyze",
    "deploy": "gcloud functions deploy analyze --runtime nodejs20 --trigger-http --allow-unauthenticated --memory 512MB --timeout 60s --region us-east1"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "@google-cloud/secret-manager": "^5.0.0",
    "mammoth": "^1.9.1",
    "openai": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "author": "AlignMyResume",
  "license": "UNLICENSED"
}