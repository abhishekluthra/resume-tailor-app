{
  "name": "insights-function",
  "version": "1.0.0", 
  "description": "Cloud Function for AI insights generation with caching",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=insights",
    "deploy": "gcloud functions deploy insights --runtime nodejs20 --trigger-http --allow-unauthenticated --memory 512MB --timeout 15s --region us-east1"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "@google-cloud/secret-manager": "^5.0.0",
    "openai": "^5.3.0",
    "redis": "^4.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "author": "AlignMyResume",
  "license": "UNLICENSED"
}